# # ─── Core ────────────────────────────────────────────────
# python-dotenv==1.0.0
# pydantic>=2.7.4,<3.0.0
# pydantic-settings==2.2.1

# # ─── LangChain / LangGraph ───────────────────────────────
# langchain==0.2.6
# langchain-openai==0.1.13
# langchain-community==0.2.6
# langchain-core>=0.2.22,<0.3.0
# langgraph==0.1.14
# langsmith==0.1.75

# # ─── Document Parsing ────────────────────────────────────
# PyMuPDF==1.24.3          # PDF text + image extraction
# pdfplumber==0.11.0       # Structured table extraction from PDFs
# camelot-py==0.11.0
# pdf2image==1.17.0        # PDF → image for visual chunks
# python-pptx==0.6.23      # PowerPoint parsing
# python-docx==1.1.0       # Word document parsing
# openpyxl==3.1.2          # Excel parsing
# Pillow==10.3.0           # Image processing

# python-jose[cryptography]==3.3.0

# ghostscript>=0.7
# opencv-python>=4.0.0
# pdfminer.six>=20200517

# # ─── Embeddings ──────────────────────────────────────────
# openai==1.35.3           # text-embedding-3-large + GPT-4o
# tiktoken==0.7.0          # Token counting

# # ─── Vector DB ───────────────────────────────────────────
# pinecone-client==3.2.2

# # ─── Metadata DB ─────────────────────────────────────────
# asyncpg==0.29.0
# sqlalchemy[asyncio]==2.0.31
# alembic==1.13.1

# # ─── Reranking ───────────────────────────────────────────
# cohere==5.5.6

# # ─── API ─────────────────────────────────────────────────
# fastapi==0.111.0
# uvicorn[standard]==0.30.1
# httpx==0.27.0

# # ─── Teams / Bot Framework ───────────────────────────────
# botbuilder-core==4.15.0
# botbuilder-integration-aiohttp==4.15.0
# botbuilder-schema==4.15.0
# msal==1.28.0             # Azure AD auth

# # ─── Observability ───────────────────────────────────────
# azure-monitor-opentelemetry==1.6.1
# opentelemetry-sdk==1.26.0
# opentelemetry-api==1.26.0
# opentelemetry-sdk==1.26.0
# opentelemetry-instrumentation==0.47b0
# opentelemetry-exporter-otlp==1.26.0
# opentelemetry-exporter-otlp-proto-grpc==1.26.0
# opentelemetry-exporter-otlp-proto-http==1.26.0
# # ─── Utilities ───────────────────────────────────────────
# numpy==1.26.4
# tqdm==4.66.4
# structlog==24.2.0
# tenacity==8.3.0          # Retry logic


# langchain-cohere==0.1.9
# Core
python-dotenv==1.0.1
pydantic>=2.7.4
pydantic-settings==2.5.2

# LangChain / LangGraph
langchain==0.2.16
langchain-openai==0.1.25
langchain-community==0.2.17
langchain-core>=0.2.22,<0.3.0
langgraph==0.2.39
# Reranking uses OpenAI (agent/nodes.py) — Cohere removed.

# Document parsing
PyMuPDF==1.24.10
pdfplumber==0.11.4
python-pptx==0.6.23
python-docx==1.1.0
openpyxl==3.1.2
Pillow==10.4.0
matplotlib==3.9.2

# Vector & DB
pinecone-client==5.0.1
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.31

# API (open access — no auth library needed)
fastapi==0.115.0
uvicorn[standard]==0.31.0
httpx==0.27.2
streamlit==1.39.0

# Utilities
numpy>=1.26.4
tqdm==4.66.5
structlog==24.4.0
tenacity==8.5.0
openai==1.50.2
tiktoken==0.7.0

# ColPali / Multimodal
byaldi==0.0.4
torch>=2.2.0
torchvision>=0.17.0
# NOTE: do NOT add `fitz` or `frontend` here — the real `fitz` module is
# provided by PyMuPDF above. The standalone `fitz` PyPI package is a
# different project that shadows it and breaks `import fitz`.